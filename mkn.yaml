#! clean build -dtKOa "-fPIC" -p test -W 9

name: mkn.mod

parent: headers

profile:
- name: headers
  inc: inc
  dep: mkn.kul parse.yaml[headers]

- name: test
  parent: headers
  test: tst/test.cpp
